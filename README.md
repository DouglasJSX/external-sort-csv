# external-sort-csv
A C implementation of External Sort applying Replacement Selection (Min-Heap) to partition a 9 GB eCommerce CSV dataset. Configured with a 200,000-record heap (~412 MB RAM), generating 170 sorted initial runs based on the 'price' column.
